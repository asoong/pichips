# PiChip Viewer

Real-time poker chip stack detection, counting, and value calculation using MobileSAM + edge detection.

## Installation

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Usage

```bash
source .venv/bin/activate
python overlay_viewer.py
```

Press `q` to quit.
